<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zaizi.mapper.EmpMapper">


    <select id="getEmp" resultType="com.zaizi.pojo.Emp">
        select id, name, image, gender, position, entry_date, last_operation_time, actions
        from emp
        <where>
            <if test="name != null and name != ''">name like concat('%',#{name},'%')</if>
            <if test="gender != null and gender != ''">and gender = #{gender}</if>
            <if test="entry_date != null">and entry_date between #{entryDate} and curdate()</if>
        </where>
    </select>
</mapper>